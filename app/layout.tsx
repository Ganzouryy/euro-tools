import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Euro-Tools | Premium Tool Rental for European Engineers in Egypt',
  description: 'Professional tool rental platform for European engineers and technicians working in Egypt. Avoid excess baggage fees and rent high-quality mechanical and electrical tools locally.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
